Flask>=3.0.0
python-escpos>=3.1
Pillow>=10.0.0
requests>=2.31.0
pyusb>=1.2.1
qrcode>=7.4.2
python-barcode>=0.15.1
gunicorn>=21.0

# dev-only (CI runs pytest; real install is fine too — few MB):
# pytest
